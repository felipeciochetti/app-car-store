export class Car {
    // tslint:disable-next-line: prettier
      // tslint:disable-next-line: prettier
    id!: number;
    brand!: string;
    model!: string;
    year!: string;
    price!: number;
}
